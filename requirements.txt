# Use the PyTorch CUDA 12.6 wheel index for PyTorch packages.
--extra-index-url https://download.pytorch.org/whl/cu126

# --- CORE ML & FINE-TUNING STACK ---
# Pinned to PyTorch 2.9.0 to match xformers dependency.
torch==2.9.0+cu126
torchvision==0.24.0+cu126
torchaudio==2.9.0+cu126

# Add NVIDIA's index to find correct CUDA-enabled binaries for the packages below.
--extra-index-url https://pypi.nvidia.com

# Pinned to a known-good, compatible set.
transformers==4.41.2
peft==0.10.0
trl==0.8.6
bitsandbytes==0.43.1
triton>=2.3.0
datasets==2.19.0
accelerate==0.30.1
xformers
llama-cpp-python

# Keras compatibility layer
tf-keras==2.20.1

# --- MODEL PUBLISHING & DEPLOYMENT ---
huggingface-hub
pyyaml

# --- CORE RAG & ORCHESTRATOR STACK ---
langchain
chromadb
google-generativeai
ollama
gpt4all